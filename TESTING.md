# TESTING.md — AFIP Test Strategy

## STEP 0: ESTABLISH THE BASELINE
No test suite currently exists. The project is at the "planning complete, code zero" stage. A test run attempting to execute `pytest` failed because the environment is completely empty (no codebase or test folder exists yet). 
**Actual Baseline:** 0 tests exist. 0 pass. 0 fail.

## CONFLICT RESOLUTION: Generic Prompt vs RULES.md
**Conflict:** The generic testing prompt requests a comprehensive suite including unit testing, integration testing, security testing (SQL injection, XSS), and performance load testing.
**Resolution:** `RULES.md` R33 explicitly mandates **"Demo-Driven Testing"**:
> "This is a hackathon. There is no time for comprehensive unit tests. Testing priorities: 1) Manual E2E for the 4 user journeys, 2) Sample data testing, 3) Offline Survival Mode, 4) SMS flow."
**Decision:** I am following **RULES.md R33**. I have completely discarded Sections 1 (Unit), 4 (Security), and 5 (Performance) from the generic prompt, as building these suites would waste critical hackathon time and violate R33. The strategy below focuses exclusively on E2E user journeys and integration of the 3-track architecture.

## SCOPE THE CHECKLIST TO THE PRODUCT TYPE
**Classification:** Web App + Service/API + Machine Learning Service.
**Skipped Checks:** As mandated by R33, I have skipped automated CI/CD unit testing, exhaustive security penetration testing, and load testing. I am focusing entirely on **End-to-End Testing (Section 3)** and **Integration Testing (Section 2)** between the Next.js frontend, FastAPI backend, and Colab ML endpoint.

---

## TEST PLAN BY RFC IMPLEMENTATION SEQUENCE

### RFC-001: Project Foundation (Phase 0)
*These tests ensure the 3-track separation can communicate before building features.*

| ID | Test Case | Priority | Expected Result | Prerequisites |
|----|-----------|----------|-----------------|---------------|
| T1.1 | **CORS Integration** - Frontend fetches dummy data from Backend | Must Have | `localhost:3000` successfully renders data from `localhost:8000` without CORS errors. | Frontend and Backend servers running. |
| T1.2 | **Seed Data Verification** | Must Have | SQLite DB contains 100 villages, 15 safe zones, and 1 admin user after running `seed.py`. | Backend environment set up. |

### RFC-002 & RFC-003: Core Engine & Map (Phase 1)
*Tests User Journey 1: The Operator Dashboard.*

| ID | Test Case | Priority | Expected Result | Prerequisites |
|----|-----------|----------|-----------------|---------------|
| T2.1 | **Risk Score Calculation** | Must Have | Moving the "River Level" slider on the UI instantly updates village risk scores. Villages > 0.7 turn Red on the map. | DB seeded with elevation data. |
| T2.2 | **Bilingual Map Labels (F11)** | Must Have | Clicking a village pin shows a popup with the name in both English and Assamese. | Map component rendered. |
| T2.3 | **Safe Zone Routing** | Must Have | Clicking a Red village draws a line to the nearest Green safe zone diamond. | Risk scores calculated. |

### RFC-004: Alerting System (Phase 2)
*Tests User Journey 2: Outbound Alerts & Inbound SOS.*

| ID | Test Case | Priority | Expected Result | Prerequisites |
|----|-----------|----------|-----------------|---------------|
| T4.1 | **Twilio SMS Dispatch (F4)** | Must Have | Clicking "Dispatch Alerts" sends an Assamese SMS to the 5 verified Twilio trial numbers. | Twilio SID/Auth Token in `.env`. |
| T4.2 | **Bhashini IVR Flow (F5)** | Should Have | Clicking "Call Farmers" triggers a phone call. Answering plays Assamese TTS audio generated by Bhashini. | Bhashini API Key in `.env`. |
| T4.3 | **SOS Webhook Loop (F15)** | Must Have | Texting "SOS [Message]" to the Twilio number creates a new red SOS pin on the dashboard map within 5 seconds. | Ngrok tunnel active for port 8000. |

### RFC-005: AI Intelligence (Phase 3)
*Tests User Journeys 3 & 4: Farmer Crop Assessment and Official Queries.*

| ID | Test Case | Priority | Expected Result | Prerequisites |
|----|-----------|----------|-----------------|---------------|
| T5.1 | **QLoRA Inference (F7)** | Must Have | Uploading a crop photo returns valid JSON (crop type, damage %, bilingual advisory) within 30 seconds. | Colab notebook running, ngrok URL in `.env`. |
| T5.2 | **Vision Fallback (R30)** | Must Have | If Colab ngrok URL is shut down, upload automatically routes to Gemini Vision and still returns a valid assessment. | Gemini API key in `.env`. |
| T5.3 | **Gov-GPT Hallucination Check (F6)** | Must Have | Asking "Which villages are safe?" returns actual Assamese village names from the DB, not generic placeholders. | 15 sample AI queries from PRD §11. |
| T5.4 | **SOS Parsing (F17)** | Must Have | Testing 20 raw Assamese/English SOS messages correctly extracts `location` and `needs` >90% of the time. | 20 sample messages from PRD §11. |

### RFC-006: Survival Polish (Phase 4)
*Tests User Journey: Offline Field Worker.*

| ID | Test Case | Priority | Expected Result | Prerequisites |
|----|-----------|----------|-----------------|---------------|
| T6.1 | **Offline Capture (F8)** | Must Have | Disabling Wi-Fi and submitting a crop assessment shows a red "Offline" banner. No crash occurs. | Chrome browser. |
| T6.2 | **IndexedDB Sync (F8)** | Must Have | Re-enabling Wi-Fi auto-sends the queued assessment and updates the DB without page reload. | Chrome browser. |
| T6.3 | **Performance Logging (F21)** | Could Have | Terminal shows request latency. All non-LLM API calls return in < 500ms (R35). | Server terminal visible. |

---

## SELF-CHECK RESULTS

1. **Table Counts:** There are 5 tables in this document representing the 5 major RFC testing phases. (Counted sequentially, no carry-overs).
2. **Feature ID Verification:** 
   - Checked F11 (Bilingual Map) -> Matches FEATURES.md.
   - Checked F4 (SMS), F5 (IVR), F15 (SOS Webhook) -> All match Phase 2 alerting.
   - Checked F7 (Crop Assessment), F6 (Gov-GPT), F17 (SOS Parse) -> All match Phase 3 AI.
   - Checked F8 (Survival Mode), F21 (Performance logging) -> All match Phase 4 polish.
3. **Rule Verification:** 
   - R33 (Demo-Driven Testing) explicitly invoked and obeyed to override generic prompt.
   - R30 (Vision Fallback) explicitly tested in T5.2.
   - R35 (Performance Targets) explicitly tested in T6.3.
4. **Internal Consistency:** No conflicts between tables. Test priorities perfectly match MoSCoW ratings from FEATURES.md.

**Self-Check Complete:** All cross-references are valid and point to correct IDs. No blind adherence to the generic prompt over the project's established RULES.md.
